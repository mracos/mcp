#!/usr/bin/env bash
# Tail daemon logs
# USAGE: mcp logs [name]

set -euo pipefail
SCRIPT_DIR="${BASH_SOURCE[0]%/*}"
source "$SCRIPT_DIR/lib-cli.bash" --auto-help "$@"
source "$SCRIPT_DIR/lib-mcp.bash"

backend_logs "${1:-}"
