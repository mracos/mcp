{
  "name": "mcp",
  "description": "Manage MCP servers from a single config file and sync to multiple AI coding clients",
  "bin": { "mcp": "./bin/mcp" },
  "license": "MIT",
  "scripts": {
    "test": "PROJECT_ROOT=$PWD bats -r test"
  },
  "devDependencies": {
    "bats": "^1.11.0",
    "bats-support": "^0.3.0",
    "bats-assert": "^2.1.0"
  }
}
