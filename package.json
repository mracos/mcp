{
  "private": true,
  "scripts": {
    "test": "PROJECT_ROOT=$PWD bats --recursive test"
  },
  "devDependencies": {
    "bats": "^1.11.0",
    "bats-support": "^0.3.0",
    "bats-assert": "^2.1.0"
  }
}
